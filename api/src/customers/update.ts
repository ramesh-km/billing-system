
import { RequestHandler } from "express";

const updateCustomerHandler: RequestHandler = async (req, res) => {
  return res.json({
    message: "Not implemented",
  });
};

export default updateCustomerHandler;
