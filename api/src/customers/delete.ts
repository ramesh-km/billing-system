import { RequestHandler } from "express";

const deleteCustomerHandler: RequestHandler = async (req, res) => {
  return res.json({
    message: "Not implemented",
  });
};

export default deleteCustomerHandler;
